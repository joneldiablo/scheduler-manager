{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "chrome-playwright": {
      "type": "local",
      "command": ["npx", "@playwright/mcp", "--browser", "chromium"],
      "enabled": true,
      "timeout": 30000
    }
  }
}
